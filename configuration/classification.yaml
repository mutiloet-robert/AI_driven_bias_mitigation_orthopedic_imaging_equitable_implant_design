seed: 1
seeds: 20
world_size: 4
device: cuda
precision: fp32
epochs: 200
batch_size_per_gpu: 32
optimizer: adamw
learning_rate: 0.0001
weight_decay: 0.00001
scheduler: cosine
image_shape: [512, 512]
lambda_fair: 0.1
lambda_bound: 0.05

